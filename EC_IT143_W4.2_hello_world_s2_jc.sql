/*******************************************************************************
** Script Name : EC_IT143_W4.2_hello_world_s2_vck.sql
** Author      : V.C.K.
** Created     : 2025-01-01
** Database    : EC_IT143_DA
** Description : W4.2 Hello World — Step 2: Begin mapping the answer
**
** The Question (from Step 1)
** --------------------------
** How many rows are in the Person.Person table?
**
** Where I am now
** --------------
** I know the table exists in the AdventureWorks-based EC_IT143_DA database.
** I know SQL Server has a COUNT() aggregate function.
** I have not yet written or tested any SQL against this table.
**
** Sub-answer map
** --------------
** Sub-answer 1 (Step 3 — ad hoc query):
**   Write a SELECT COUNT(*) against Person.Person.
**   Confirm the table is accessible and returns a numeric result.
**
** Sub-answer 2 (Step 4 — view):
**   Wrap that COUNT query in a CREATE VIEW so it is reusable
**   and lives inside the database.
**
** Sub-answer 3 (Steps 5–8):
**   Persist the result into a physical table, write a load procedure,
**   and call it — completing the full ETL pipeline.
**
** I am not yet concerned with Steps 3–5 in detail.
** The sub-answer map will evolve as I learn more.
**
** Revision History
** ----------------
** 2025-01-01  vck  Initial creation
*******************************************************************************/

-- Step 2: No executable SQL yet.
-- This script documents the answer map before any code is written.

-- Sub-answer 1: SELECT COUNT(*) FROM Person.Person
-- Sub-answer 2: CREATE VIEW wrapping that SELECT
-- Sub-answer 3: Physical table + stored procedure (to be refined in Steps 5–8)
